require "spec_helper"

RSpec.describe Bootpay do
  it "has a version number" do
    expect(Bootpay::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
